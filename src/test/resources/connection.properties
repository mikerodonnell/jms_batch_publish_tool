
# this brokerUrl is what's returned by EmbeddedActiveMQBroker#.getVmURL(); the failover transport isn't really necessary, but keeping default EmbeddedActiveMQBroker value
activemq.broker_url=failover:(vm://embedded-broker?create=false)

activemq.outbound_queue_name=TEST.QUEUE
